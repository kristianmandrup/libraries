// Generated by LiveScript 1.2.0
/**
 * User: kristianmandrup
 * Date: 17/10/14
 * Time: 21:16
 */
(function(){
  var BaseInstaller, toString$ = {}.toString;
  module.exports = BaseInstaller = (function(){
    BaseInstaller.displayName = 'BaseInstaller';
    var prototype = BaseInstaller.prototype, constructor = BaseInstaller;
    function BaseInstaller(name, content, file, options){
      this.name = name;
      this.content = content;
      this.file = file;
      this.options = options != null
        ? options
        : {};
      this.validate();
      this.log = this.options.log || console.log;
    }
    prototype.validate = function(){
      if (toString$.call(this.name).slice(8, -1) !== 'String') {
        throw new Error("Name must be a String, was: " + this.name);
      }
      if (toString$.call(this.content).slice(8, -1) !== 'String') {
        throw new Error("Source of config must be a String, was: " + this.content);
      }
    };
    prototype.installing = function(){
      return this.log("installing: " + this.name);
    };
    prototype.uninstalling = function(){
      return this.log("uninstalling: " + this.name);
    };
    return BaseInstaller;
  }());
}).call(this);
