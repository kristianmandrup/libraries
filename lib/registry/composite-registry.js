// Generated by LiveScript 1.2.0
(function(){
  var Registry, CompositeRegistry;
  Registry = require('./registry');
  module.exports = CompositeRegistry = (function(){
    CompositeRegistry.displayName = 'CompositeRegistry';
    var prototype = CompositeRegistry.prototype, constructor = CompositeRegistry;
    function CompositeRegistry(optionsList){
      this.optionsList = optionsList != null
        ? optionsList
        : [];
      this.validate();
      this;
    }
    prototype.validate = function(){};
    prototype.registries = [];
    prototype.configure = function(){
      var i$, ref$, len$, options, results$ = [];
      for (i$ = 0, len$ = (ref$ = this.optionsList).length; i$ < len$; ++i$) {
        options = ref$[i$];
        results$.push(this.registries.push(this.registry(options)));
      }
      return results$;
    };
    prototype.registry = function(options){
      return new Registry(options);
    };
    return CompositeRegistry;
  }());
}).call(this);
