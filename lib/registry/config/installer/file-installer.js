// Generated by LiveScript 1.2.0
(function(){
  var fs, jsonlint, FileIO, BaseInstaller, FileInstaller, toString$ = {}.toString;
  fs = require('fs-extra');
  jsonlint = require('jsonlint');
  FileIO = require('../../../util/file-io');
  BaseInstaller = require('./base-installer');
  module.exports = FileInstaller = (function(superclass){
    var prototype = extend$((import$(FileInstaller, superclass).displayName = 'FileInstaller', FileInstaller), superclass).prototype, constructor = FileInstaller;
    importAll$(prototype, arguments[1]);
    function FileInstaller(name, content, options){
      this.name = name;
      this.content = content;
      this.options = options != null
        ? options
        : {};
      FileInstaller.superclass.apply(this, arguments);
    }
    prototype.validate = function(){
      superclass.prototype.validate.call(this);
      if (toString$.call(this.content).slice(8, -1) !== 'String') {
        throw new Error("Source of config must be a String, was: " + this.content);
      }
    };
    prototype.install = function(){
      var err;
      if (this.exists(this.file)) {
        return;
      }
      try {
        this.installing;
        this.writeFile();
        return this.name;
      } catch (e$) {
        err = e$;
        this.error(err);
      }
    };
    prototype.uninstall = function(){
      var err;
      try {
        this.uninstalling;
        return this.removeFile();
      } catch (e$) {
        err = e$;
        return this.error(err);
      }
    };
    return FileInstaller;
  }(BaseInstaller, FileIO));
  function extend$(sub, sup){
    function fun(){} fun.prototype = (sub.superclass = sup).prototype;
    (sub.prototype = new fun).constructor = sub;
    if (typeof sup.extended == 'function') sup.extended(sub);
    return sub;
  }
  function import$(obj, src){
    var own = {}.hasOwnProperty;
    for (var key in src) if (own.call(src, key)) obj[key] = src[key];
    return obj;
  }
  function importAll$(obj, src){
    for (var key in src) obj[key] = src[key];
    return obj;
  }
}).call(this);
