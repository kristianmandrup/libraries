// Generated by LiveScript 1.2.0
(function(){
  var co, search, warning, registry, Q, BaseComponentAdapter, LocalComponentAdapter;
  co = require('co');
  search = co(require('component-search2'));
  warning = function(msg){
    throw Error(msg);
  };
  registry = {
    search: function(query, cb){
      var location;
      location = ['./components', query.name, 'component.json'];
      return fs.readFile(location, 'utf-8', function(err, pkg){
        if (err) {
          cb(err);
        }
        if (!pkgs.length) {
          cb('no matching components found');
        }
        return cb(null, [jsonlint.parse(pkg)]);
      });
    }
  };
  Q = require('q');
  BaseComponentAdapter = require('./base-component');
  module.exports = LocalComponentAdapter = (function(superclass){
    var prototype = extend$((import$(LocalComponentAdapter, superclass).displayName = 'LocalComponentAdapter', LocalComponentAdapter), superclass).prototype, constructor = LocalComponentAdapter;
    function LocalComponentAdapter(name, options){
      this.name = name;
      this.options = options != null
        ? options
        : {};
      LocalComponentAdapter.superclass.apply(this, arguments);
    }
    prototype.query = function(){
      return this._query || (this._query = {
        name: this.name
      });
    };
    return LocalComponentAdapter;
  }(BaseComponentAdapter));
  function extend$(sub, sup){
    function fun(){} fun.prototype = (sub.superclass = sup).prototype;
    (sub.prototype = new fun).constructor = sub;
    if (typeof sup.extended == 'function') sup.extended(sub);
    return sub;
  }
  function import$(obj, src){
    var own = {}.hasOwnProperty;
    for (var key in src) if (own.call(src, key)) obj[key] = src[key];
    return obj;
  }
}).call(this);
