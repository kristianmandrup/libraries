// Generated by LiveScript 1.2.0
/**
 * User: kristianmandrup
 * Date: 06/10/14
 * Time: 14:06
 */
(function(){
  var FileAdapter, UriAdapter, Registry;
  FileAdapter = require('./adapter/file-adapter');
  UriAdapter = require('./adapter/uri-adapter');
  module.exports = Registry = (function(){
    Registry.displayName = 'Registry';
    var prototype = Registry.prototype, constructor = Registry;
    function Registry(type, options){
      this.type = type;
      this.options = options != null
        ? options
        : {};
    }
    prototype.adapter = function(){
      return new this.selectedAdapter()(this.options);
    };
    prototype.selectAdapter = function(){
      switch (this.type) {
      case 'file':
        return FileAdapter;
      case 'uri':
        return UriAdapter;
      default:
        throw new Error("unknown Registry adapter " + this.type);
      }
    };
    return Registry;
  }());
}).call(this);
