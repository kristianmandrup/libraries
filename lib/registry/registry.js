// Generated by LiveScript 1.2.0
/**
 * User: kristianmandrup
 * Date: 06/10/14
 * Time: 14:06
 */
(function(){
  var FileIO, Registry, toString$ = {}.toString;
  FileIO = require('file-io');
  module.exports = Registry = (function(){
    Registry.displayName = 'Registry';
    var prototype = Registry.prototype, constructor = Registry;
    importAll$(prototype, arguments[0]);
    function Registry(uriRoot, targetPath){
      this.uriRoot = uriRoot;
      this.targetPath = targetPath;
      this.registryUri || (this.registryUri = './xlibs/registry');
      this.localRegistryPath || (this.localRegistryPath = this.uriRoot);
      this.validate();
      this;
    }
    prototype.validate = function(){
      if (toString$.call(this.registryUri).slice(8, -1) !== 'String') {
        throw new Error("uri root must be a String");
      }
    };
    prototype.indexFile = function(){
      return [this.registryUri, 'index.json'].join('/');
    };
    prototype.index = function(){
      return this.read(indexFile);
    };
    prototype.configFile = function(name){
      return [this.registryUri, name + ".json"].join('/');
    };
    prototype.targetConfig = function(name){
      return [this.localRegistryPath, name + ".json"].join('/');
    };
    prototype.install = function(name, targetPath){
      this.targetPath = targetPath != null ? targetPath : './xlibs/components';
      return fs.copy(this.configFile(name), this.localRegistryPath(name));
    };
    prototype.uninstall = function(name, targetPath){
      this.targetPath = targetPath != null ? targetPath : './xlibs/components';
      return fs['delete'](this.targetConfig(name));
    };
    return Registry;
  }(FileIO));
  function importAll$(obj, src){
    for (var key in src) obj[key] = src[key];
    return obj;
  }
}).call(this);
