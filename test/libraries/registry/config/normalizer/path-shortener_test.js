// Generated by LiveScript 1.2.0
(function(){
  var PathShortener;
  PathShortener = require('../../../../lib/registry/config/normalizer/path-shortener');
  describe('PathShortener', function(){
    var config;
    config = {};
    config.simple = {
      main: {
        files: ['dist/js/bootstrap.js']
      },
      scripts: {
        dir: 'dist/js',
        files: ['bootstrap.js']
      }
    };
    describe('create(config)', function(){});
    return context('instance', function(){
      var shortener;
      beforeEach(function(){
        return shortener = new PathShortener(config.simple);
      });
      describe('shorten-paths', function(){
        return specify('shortens all paths', function(){
          return expect(shortener.shortener()).to.eql('bootstrap.js');
        });
      });
      describe('shorten-paths-for(entry)', function(){});
      return describe('shorten-path(file)', function(){});
    });
  });
}).call(this);
