// Generated by LiveScript 1.2.0
(function(){
  var expect, log, DirShortener;
  expect = require('chai').expect;
  log = console.log;
  DirShortener = require('../../../../../lib/registry/config/normalizer/dir-shortener');
  describe('DirShortener', function(){
    var config;
    config = {};
    config.simple = {
      dir: 'dist/js',
      main: {
        files: ['dist/js/bootstrap.js']
      },
      scripts: {
        dir: 'dist/js',
        files: ['bootstrap.js']
      }
    };
    describe('create(config)', function(){});
    return context('instance', function(){
      var shortener;
      beforeEach(function(){
        return shortener = new DirShortener(config.simple.scripts);
      });
      return describe('shorten-dir', function(){
        return specify('shortens dir path', function(){
          var config;
          config = shortener.shortenDir('dist');
          log(config);
          return expect(config.dir).to.eql('js');
        });
      });
    });
  });
}).call(this);
