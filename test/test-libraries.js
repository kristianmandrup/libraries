// Generated by LiveScript 1.2.0
(function(){
  var app, Libraries, libraries, origFile, file, fs, copyFile;
  app = {
    'import': function(location, opts){
      var x;
      opts == null && (opts = '');
      x = opts ? '", ' : '"';
      return console.log('app.import(  "' + location + x, opts, ');');
    },
    bowerDirectory: 'bower_components'
  };
  Libraries = require('../libraries');
  libraries = new Libraries({
    app: app
  });
  origFile = './imports/libraries.json';
  file = './imports/libraries.json';
  fs = require('fs');
  copyFile = function(source, target){
    return fs.writeFileSync(target, fs.readFileSync(source));
  };
  copyFile(origFile, file);
  libraries.importAll(app, {
    file: file
  });
  console.log('with options');
  libraries.importAll(app, {
    file: './imports/libraries.json',
    config: {
      vendor: 'vendor/dev'
    }
  });
  libraries.addLibs('bower', 'dist/ember-validations.js');
  libraries.addLibs('vendor', ['v.js', 'z.js']);
  libraries.addLibs({
    bower: 'blip/blap.js'
  });
  libraries.addRemaps('bower', {
    'jquery/core': 'dist/core/jquery.js',
    'famous/core': 'famous/dist/core/famous.js'
  });
  libraries.removeLib('bower', 'dist/ember-validations.js');
  libraries.removeRemaps('bower', 'jquery/core');
  libraries.print();
}).call(this);
