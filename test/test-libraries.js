// Generated by LiveScript 1.2.0
(function(){
  var app, libraries, file, Adder, adder;
  app = {
    'import': function(location, opts){
      var x;
      opts == null && (opts = '');
      x = opts ? '", ' : '"';
      return console.log('app.import(  "' + location + x, opts, ');');
    },
    bowerDirectory: 'bower_components'
  };
  libraries = require('../libraries');
  libraries.importAll(app);
  file = './imports/libraries.json';
  libraries.importAll(app, {
    file: file
  });
  console.log('with options');
  libraries.importAll(app, {
    file: './imports/libraries.json',
    config: {
      vendor: 'vendor/dev'
    }
  });
  Adder = require('../adder');
  adder = new Adder({
    file: file
  });
  adder.addLibs('bower', 'dist/ember-validations.js');
  adder.addLibs('vendor', ['v.js', 'z.js']);
  adder.addLibs({
    bower: 'blip/blap.js'
  });
  adder.addRemaps('bower', {
    'jquery/core': 'dist/core/jquery.js'
  });
  console.log('libs', adder.libs);
}).call(this);
