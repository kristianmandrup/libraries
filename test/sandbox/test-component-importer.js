// Generated by LiveScript 1.2.0
(function(){
  var app, Importer, fs, compFile, readLibs, components, obj, importer;
  app = {
    'import': function(location, opts){
      var x;
      opts == null && (opts = '');
      x = opts ? '", ' : '"';
      return console.log('app.import(  "' + location + x, opts, ');');
    },
    bowerDirectory: 'bower_components'
  };
  Importer = require('../component-importer');
  fs = require('fs');
  compFile = './xlibs/component.json';
  readLibs = function(file){
    return fs.readFileSync(file, 'utf8');
  };
  components = JSON.parse(readLibs(compFile));
  obj = {
    app: app,
    directory: app.bowerDirectory
  };
  importer = new Importer(obj, components);
  importer.importAll();
}).call(this);
